{
  "name": "parser",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "packages/parser/src",
  "projectType": "library",
  "tags": [],
  "targets": {
    "compile": {
      "executor": "nx:run-commands",
      "options": {
        "commands": ["npx tree-sitter generate -0"],
        "cwd": "packages/parser"
      },
      "outputs": ["{workspaceRoot}/packages/parser"]
    },
    "build": {
      "dependsOn": ["compile"],
      "executor": "@nrwl/js:tsc",
      "outputs": ["{options.outputPath}"],
      "options": {
        "outputPath": "dist/packages/node_modules/parser",
        "main": "packages/parser/src/index.ts",
        "tsConfig": "packages/parser/tsconfig.lib.json",
        "assets": [
          "packages/parser/*.md",
          "packages/parser/bindings/",
          "packages/parser/src/",
          "packages/parser/binding.gyp",
          "packages/parser/Cargo.toml"
        ]
      }
    },
    "lint": {
      "executor": "@nrwl/linter:eslint",
      "outputs": ["{options.outputFile}"],
      "options": {
        "lintFilePatterns": ["packages/parser/**/*.ts"]
      }
    },
    "test": {
      "dependsOn": ["build"],
      "executor": "nx:run-commands",
      "options": {
        "commands": ["npx tree-sitter test"],
        "cwd": "packages/parser"
      },
      "configurations": {
        "debug": {
          "commands": ["npx tree-sitter test -d"]
        }
      },
      "inputs": ["{projectRoot}/corpus"]
    }
  }
}
